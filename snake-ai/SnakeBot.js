module.exports = {
    getNextMove: function (data) {

        var response = {
            move: 'down', // one of: ['up','down','left','right']
            taunt: "Moving...." // optional, but encouraged!
          }
        return response;
    }
}